<?php
/**
 * Homepage - hero → difference is execution → 6 agents → integration marquee →
 * 4 steps (condensed) → metrics → CTA. CLAUDE.md §5.
 */

get_header();
get_template_part( 'parts/home' );
get_footer();
